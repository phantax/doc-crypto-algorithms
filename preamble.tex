\documentclass[
	fontsize=11pt,
	paper=A4,
	parskip=half,
	DIV=calc,
	headinclude=true,		
	footinclude=true,
	%open=right,
	appendixprefix=true,	% include appendix?
	bibliography=totoc,		% include an unnumbered unit of bibliography to the table of contents
	BCOR=10mm,        		% binding correction (depends on how you bind
	                     	% the resulting printout.
	oneside          		% alternative: twoside
]{scrbook}

\usepackage[				% citation management
	backend=bibtex,
	style=numeric-comp,
	%backref=true, 			% create backlings from references to citations
	natbib=true, 			% offering natbib-compatible commands
	hyperref=true, 			% using hyperref-package references
    maxcitenames=2,
    sorting=none
]{biblatex}					% http://ctan.mirrorcatalogs.com/macros/latex/contrib/biblatex/doc/biblatex.pdf

\addbibresource{Bibliography.bib} % file with the citations


\usepackage[decimalsymbol=comma, emulate=units]{siunitx}	% simplified usage of SI-units 
															% ftp://ftp.tex.ac.uk/ctan%3A/macros/latex/exptl/siunitx/siunitx.pdf
\sisetup{locale=DE, tophrase={{ bis }}}						% customize some settings of the SI-units for german language
\usepackage[automark]{scrpage2}								% allows usage of header and footer	
\usepackage{lastpage}										% get total number of pages
\usepackage{graphicx}										% includes pictures
\usepackage{tabularx}										% creates tables
\usepackage{calc}											
\usepackage[english]{babel}									% language adaptions (options: english, spanish, swedish, portugues)
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[table]{xcolor}									% includes some colors

\usepackage{longtable}

\usepackage{epstopdf}
\usepackage{booktabs}
\usepackage{rotating}
\usepackage{listings}
\usepackage{lstpatch}
											% http://www.tex.ac.uk/ctan/macros/latex/contrib/todonotes/todonotes.pdf
\usepackage{csquotes}
\usepackage[perpage, hang]{footmisc} 						% footnote options
% perpage: Nummerierung der Fußnoten auf jeder Seite neu beginnen
% hang: Fußnote über mehrere zeilen richtig einrücken
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{listings}				% include source code
									% ftp://ftp.tex.ac.uk/tex-archive/macros/latex/contrib/listings/listings.pdf
\usepackage{color}

\definecolor{light-gray}{gray}{0.95}	% define some colors
\definecolor{dark-green}{rgb}{0,0.6,0}
\lstset{ 							% options for representation of source code
  backgroundcolor=\color{light-gray},   % choose the background color; you must add \usepackage{color} or \usepackage{xcolor}
  basicstyle=\footnotesize,        % the size of the fonts that are used for the code
  breakatwhitespace=false,         % sets if automatic breaks should only happen at whitespace
  breaklines=true,                 % sets automatic line breaking
  captionpos=b,                    % ses the caption-position to bottom
  commentstyle=\color{dark-green}, % comment style
  deletekeywords={...},            % if you want to delete keywords from the given language
  escapeinside={\%*}{*)},          % if you want to add LaTeX within your code
  extendedchars=true,              % lets you use non-ASCII characters; for 8-bits encodings only, does not work with UTF-8
  frame=lines,                     % adds a frame around the code
  keepspaces=true,                 % keeps spaces in text, useful for keeping indentation of code (possibly needs columns=flexible)
  keywordstyle=\color{blue},       % keyword style
  language=C,                      % the language of the code
  morekeywords={*,...},            % if you want to add more keywords to the set
  numbers=left,                    % where to put the line-numbers; possible values are (none, left, right)
  numbersep=8pt,                   % how far the line-numbers are from the code
  numberstyle=\tiny\color{gray},   % the style that is used for the line-numbers
  rulecolor=\color{black},         % if not set, the frame-color may be changed on line-breaks within not-black text (e.g. comments (green here))
  showspaces=false,                % show spaces everywhere adding particular underscores; it overrides 'showstringspaces'
  showstringspaces=false,          % underline spaces within strings only
  showtabs=false,                  % show tabs within strings adding particular underscores
  stepnumber=1,                    % the step between two line-numbers. If it's 1, each line will be numbered
  stringstyle=\color{blue},        % string literal style
  tabsize=2,                       % sets default tabsize to 2 spaces
  title=\lstname,                  % show the filename of files included with \lstinputlisting; also try caption instead of title
  %numberbychapter=false
}
\renewcommand\lstlistingname{Quelltext}	% change caption of listings
%\usepackage{chngcntr}				% ongoing numbering of...
%\counterwithout{figure}{chapter}   % ... pictures
%\counterwithout{table}{chapter} 	% ... tables
%\counterwithout{equation}{chapter} % ... equations
\usepackage[]{acronym}				% for usage of abbreviations
\usepackage{setspace}				% set line distance...
\onehalfspacing						% ... to 1.5
\addtokomafont{caption}{\small} 	% small captions




\pdfcompresslevel=9 

% change page layout
% ftp://ftp.fu-berlin.de/tex/CTAN/macros/latex/contrib/geometry/geometry.pdf
\addtolength{\oddsidemargin}{-.875in}
\addtolength{\evensidemargin}{-.875in}
\addtolength{\textwidth}{1.75in}
\addtolength{\topmargin}{-.875in}
\addtolength{\textheight}{3in}  %1.75

% header and footer
\pagestyle{scrheadings}							% for customization for header and footer
\renewcommand{\chapterpagestyle}{scrheadings}	% include header and footer on chapter pages
\clearscrheadfoot

% http://ctan.uib.no/macros/latex/contrib/koma-script/doc/scrguien.pdf
% page 204
\ifoot{\vspace{-0.25cm}Andreas Walz}

\ofoot{ \vspace{-0.25cm} \thepage}
\automark{chapter}
\setheadsepline[ \textwidth + 5pt ]{1pt} % seperation line for header...
\setfootsepline[ \textwidth + 5pt ]{1pt} % ... and footer

\setkomafont{footsepline}{\color{black}} 	 % change colors of seperation lines
\setkomafont{headsepline}{\color{black}}

\usepackage[font=small, width=0.9\textwidth, format=plain, labelfont=bf]{caption}

\usepackage[							% enables typesettings for hyperlinks
										% http://en.wikibooks.org/wiki/LaTeX/Hyperlinks
	colorlinks=false,					% removes color frames 
	%hidelinks,							% removes color and porder
	%breaklinks=true,
	pdftitle = {Cryptographic Algorithms},
	pdfauthor = {Andreas Walz},
	pdfsubject = {},
]{hyperref}								% should be the last package to be inlcuded!


